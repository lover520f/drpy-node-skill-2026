# 需求分析文档

## 本轮补充说明

本轮任务只完善 `trans_result` 下已有 Markdown 文档，把重新学习 drpy-node 关键源码、MCP 工具源码和现有 JS 源得到的新知识补回知识库。4 个 skill 的优化记录保留为历史背景，本轮不修改 skill 文件；除非后续发现现有文档完全无法承载新主题，否则不新增文档。

## 一、总体目标

通过学习 drpy-node 项目的框架源码和现有的 187 个 JS 源，掌握 drpy DS 源的编写方法论，
输出系统性学习总结，并基于此优化现有的 4 个 skill，大幅提升 AI 写源能力。

## 二、需求拆解

### 2.1 学习阶段（训练）

#### 2.1.1 框架核心源码学习
- **目标文件**：`libs/drpyS.js`、`libs/drpysParser.js`、`libs_drpy/template.js`
- **关键理解点**：
  - 沙箱（sandbox）隔离机制：`vm.createContext` + `vm.Script`
  - 模块初始化流程：`init()` → `handleTemplateInheritance()` → `initParse()`
  - 模板继承机制：rule.模板 → `template.getMubans()` → `Object.assign` 合并
  - 调度分发机制：`invokeMethod()` → 根据 method 类型和 rule 字段类型（string/function/js:/*）分流
  - 字符串规则解析：`commonCategoryListParse`、`commonClassParse`、`commonDetailListParse` 等
  - 播放链路：`lazy` 的 `parse:0/1`、`jx:0/1` 返回值语义
- **学习产出**：理解 drpy 框架的设计哲学和运行机制

#### 2.1.2 源文件风格学习
- **目标目录**：`spider/js/` 下 187 个 JS 源
- **重点分析维度**：
  - **模板继承型**（如 `樱花动漫[优].js`）：极少量代码，`模板:'mxpro'` 覆盖个别字段
  - **字符串规则型**（如 `DJ音乐[听].js`）：CSS 选择器字符串描述列表/详情
  - **函数驱动型**（如 `独播库[优].js`）：全 async function，API 签名逻辑
  - **混合型**（如 `360影视[官].js`）：部分字段用字符串，复杂逻辑用函数
  - **网盘型**（如 `网盘[模板].js`）：多网盘转存/解析逻辑
  - **模板基础文件**（`_base.js`、`_base1[画].js`、`_base2[画].js`）
  - **工具库文件**（`_lib.*.js`、`_fq.js`、`_360.js`、`_qq.js`）
- **学习产出**：掌握各种风格的最佳实践和适用场景

### 2.2 技能优化目标

#### 2.2.1 现有 4 个 skill 分析
当前 skill 列表：
| Skill | 职责 | 质量评估 |
|-------|------|---------|
| `drpy-node-source-create` | 从零新建源 | 良好，已有模板/API/SPA 三类路线 |
| `drpy-node-source-workflow` | 修源/测试/上传总控 | 良好，分流机制清晰 |
| `drpy-node-play-debug` | 播放链排障 | 良好，决策树完整 |
| `drpy-node-repo-upload` | 仓库上传 | 良好，红线机制明确 |

#### 2.2.2 优化方向
1. **加深框架理解**：现有 skill 偏重"怎么做"，缺少"为什么"。需补充框架内部机制。
2. **补齐模板系统知识**：当前 skill 提到了模板继承但未深入 template.js 的 12+ 种模板细节。
3. **补齐 drpy API 知识**：利用 MCP 的 `get_drpy_libs_info`、`get_drpy_api_list` 增强知识面。
4. **补齐高级用法**：`hostJs`、`预处理`、`模板修改`、`filter` 压缩编码等。
5. **MCP 能力深挖**：理解 MCP 源码中各工具的边界，更好配合使用。

### 2.3 MCP 源码学习

#### 2.3.1 关键文件
- `/e/gitwork/drpy-node/drpy-node-mcp/index.js` — MCP 服务器入口
- `/e/gitwork/drpy-node/drpy-node-mcp/tools/` — 工具实现
  - `fsTools.js` — 源文件操作（读/写/编辑/删除/搜索/列目录，含 DS 解密与 JS 语法校验）
  - `spiderTools.js` — 网站分析、模板判断、规则调试、语法/结构验证、resolved rule 等
  - `spiderTestTools.js` — 通过 localDsCore 调用引擎测试
  - `extractFilter.js` — 筛选条件提取
  - `houseTools.js` — 仓库操作
  - `drpyLibsInfo.js` — drpy 库信息
  - `systemTools.js` — 系统管理
  - `dbTools.js` — 数据库操作
- `skills.md` / `skills-zh.md` — MCP 侧的编写指南

#### 2.3.2 学习目标
- 理解 MCP 工具的能力边界
- 评估是否需要新增/修改工具来增强 AI 写源能力

### 2.4 Playwright 浏览器验证

学习过程中需要调用 Playwright MCP 进行实际的网站分析和验证：
1. `analyze_website_structure(url)` 抓取 DOM 结构
2. `fetch_spider_url(url)` 测试 API 响应
3. `extract_iframe_src(url)` 提取播放页
4. `browser_navigate` + `browser_snapshot` 浏览器级分析

## 三、执行计划

### 阶段一：深度源码分析
- [x] 读取 `drpyS.js` 理解框架核心
- [x] 读取 `template.js` 理解模板系统
- [x] 读取 `drpysParser.js` 理解解析器（invokeWithInjectVars Proxy、playParseAfter、buildVodObject）
- [x] 读取 `localDsCore` 理解引擎调用
- [x] 读取控制器层（`controllers/`）理解 API 路由

### 阶段二：源文件分类学习
- [x] 模板继承最小量示例分析（如 `樱花动漫[优].js`）
- [x] 函数驱动复杂源分析（如 `独播库[优].js`、`360影视[官].js`）
- [x] 网盘源分析（如 `网盘[模板].js`）
- [x] 漫画/音乐/小说/听书类特殊源分析（`_base1[画].js`、`DJ音乐[听].js`、`番茄小说[书].js`）
- [x] 工具库文件分析

### 阶段三：MCP 源码学习
- [x] 通读 MCP 所有工具实现（index.js, toolDefinitions.js, 7 个工具模块）
- [x] 评估工具增删改需求（当前工具集完整，无新增必要）

### 阶段四：Skill 优化
- [x] 基于学习成果优化 `drpy-node-source-create`（新增 template-system.md, api-functions.md, special-content.md, 12模板选择指南, 5种编写模式, 特殊内容类型）
- [x] 优化 `drpy-node-source-workflow`（新增 references-framework-internals.md, B类深层机制源码级解释）
- [x] 优化 `drpy-node-play-debug`（增强 references-play-lazy-summary.md: playParseAfter, 加密模式, 特殊协议, 多线路）
- [x] 优化 `drpy-node-repo-upload`（增强 references-upload-decision.md: L1/L2/L3验证深度, 自动标签检测）
- [x] 输出综合性学习总结文档（learning-summary.md）
- [x] 输出完整知识体系文档（framework-core.md, template-system.md, source-patterns.md, api-reference.md, mcp-tool-map.md）
- [x] 所有 skill 自包含：知识从 trans_result 内化到各 skill references/，无外部依赖

### 阶段五：MCP 改进（可选）
- [x] 如有必要修改 MCP 服务源码，增删工具（当前评估：工具集完整，无需新增）
- [x] MCP skills.md/skills-zh.md 已重写为完整知识体系
- [x] 无需重新安装配置（仅修改文档文件，未改动源码）

## 四、输出物清单

| 输出 | 路径 | 说明 |
|------|------|------|
| 需求文档 | `trans_result/需求.md` | 本文件 |
| drpy 框架核心机制总结 | `trans_result/drpy-framework-core.md` | 沙箱、模板继承、调度分发的深度解析 |
| 模板系统详解 | `trans_result/template-system.md` | 12+ 模板的细节和选用指南 |
| 源文件编写模式分析 | `trans_result/source-patterns.md` | 各种编写风格的范例和场景 |
| drpy API 与全局函数参考 | `trans_result/drpy-api-reference.md` | pdfh/pd/pda/req/setResult 等完整参考 |
| MCP 工具能力图谱 | `trans_result/mcp-tool-map.md` | 工具能力边界和使用场景 |
| 综合性学习总结 | `trans_result/learning-summary.md` | 写给 AI 的写源知识体系 |
| 优化后 skill (×4) | `skills/` 目录 | 更新现有 4 个 skill |

## 五、长期运行方案

由于任务量大，采用分阶段执行策略：
- 阶段一/二：同步执行，每完成一个学习点输出对应 md
- 阶段三/四：基于前两阶段的输出进行 skill 优化
- 阶段五：按需执行

每个阶段完成自动进入下一阶段，直到全部输出和优化完成。
